import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ygy.braintraining',
  appName: 'Ygy Brain Training',
  webDir: 'dist'
};

export default config;
