class Achievement {
  String id;
  String title;
  String description;
  String type;
  bool unlocked;

  Achievement({
    required this.id,
    required this.title,
    required this.description,
    required this.type,
    required this.unlocked,
  });
}

